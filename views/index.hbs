<body>
    <h1>MyRestaurant</h1>
    <div class="flexbox">
        <div class="box">
            <ul class="restaurants">
                {{#each restaurants}}
                <li class="restaurants-item">
                    <img src="{{this.imageURL}}" class="restaurants-item-image">
                    <div class="restaurants-item-name">{{this.name}}</div>
                    <div class="restaurants-item-type">{{this.type}}</div>
                </li>
                {{/each}}
            </ul>
        </div>
    </div>
</body>